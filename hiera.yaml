---
:backends: 
  - yaml
:yaml:
  :datadir: /etc/puppet/hieradata
:hierarchy:
  - "%{::deployment_pattern}/hosts"
  - "%{::deployment_pattern}/%{::clientcert}"
  - database
  - datasources
  - common
